$('.spotify-tracks-errors-' + <%= current_user.id %>).html('<%= render 'shared/errors', resource: @track %>');
$('.tracks-' + <%= current_user.id %>).html('<%= render 'spotify_tracks/tracks', resource: current_user %>');
